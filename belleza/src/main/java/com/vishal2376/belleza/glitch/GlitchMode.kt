package com.vishal2376.belleza.glitch

/**
 * Determines how glitch animation behaves.
 */
enum class GlitchMode {
	/**
	 * Glitch effect runs once for a set duration.
	 */
	ONCE,

	/**
	 * Glitch runs continuously until stopped.
	 */
	LOOP
}
